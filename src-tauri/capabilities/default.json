{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the MigraDry window. The frontend may open a file picker and nothing else; every filesystem and SQLite operation happens in Rust behind the single `preview_migration` command.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
